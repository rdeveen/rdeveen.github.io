// For format details, see https://aka.ms/devcontainer.json. For config options, see the
// README at: https://github.com/devcontainers/templates/tree/main/src/jekyll
{
	"name": "Jekyll",
	// Or use a Dockerfile or Docker Compose file. More info: https://containers.dev/guide/dockerfile
	"image": "mcr.microsoft.com/devcontainers/jekyll",

	// Features to add to the dev container. More info: https://containers.dev/features
	"features": {
    // https://github.com/devcontainers-community/npm-features/tree/main/src/prettier
    "ghcr.io/devcontainers-community/npm-features/prettier": {}
  },

	// Use 'forwardPorts' to make a list of ports inside the container available locally.
	"forwardPorts": [
		4000
	],
	"customizations": {
		"vscode": {
			"extensions": [
				"GitHub.copilot",
				"davidanson.vscode-markdownlint",
				"KoichiSasada.vscode-rdbg",
				"ginfuru.ginfuru-vscode-jekyll-syntax"
			]
		}
	},

	// Uncomment the next line to run commands after the container is created.
	"postCreateCommand": "bundle exec jekyll serve --force_polling --incremental --watch"

	// Configure tool-specific properties.
	// "customizations": {},

	// Uncomment to connect as root instead. More info: https://aka.ms/dev-containers-non-root.
	// "remoteUser": "root"
}
